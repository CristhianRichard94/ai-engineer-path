# SECURITY WARNING: NEXT_PUBLIC_* variables are embedded in the public JS bundle
# and are visible to anyone who opens DevTools in their browser.
# Use a fine-grained personal access token scoped to read-only public repo access only.
# Never use a token that has write permissions or access to private repositories.
NEXT_PUBLIC_GITHUB_TOKEN=
